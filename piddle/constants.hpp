#define COUNT_OF(x) (sizeof(x) / sizeof(0[x]))

const int LED_COUNT = 40;

const int BUTTON_PIN = 34;
const int MICROPHONE_ANALOG_PIN = 26;

// For testing, just define 1
const int LED_PIN = 23;
const int LED_PINS[] = {15, 23};
